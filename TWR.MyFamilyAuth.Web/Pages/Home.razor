@page "/"
@attribute [Authorize]

<PageTitle>Dashboard — MyFamilyAuth</PageTitle>

<h4 class="mb-3"><i class="bi bi-speedometer2 me-2 text-primary"></i>Dashboard</h4>

<div class="row g-3">
    <div class="col-12">
        <div class="card shadow-sm">
            <div class="card-body text-center py-5 text-muted">
                <i class="bi bi-people-fill fs-1 d-block mb-3 opacity-25"></i>
                <p>Welcome to MyFamilyAuth — the identity hub for TWR MyApps.</p>
            </div>
        </div>
    </div>
</div>
